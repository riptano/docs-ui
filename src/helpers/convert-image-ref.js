module.exports = (string, { data }) => {
  // console.log(data)
  // const { contentCatalog, page } = data.root
  // if (!contentCatalog) return
  // const convertImageRef = require('@antora/asciidoc-loader/lib/image/convert-image-ref.js')
  // return convertImageRef(string, page, contentCatalog)
}

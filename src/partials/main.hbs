<div class="flex flex-col">
  <div class="flex m-0 xl:mx-auto px-3 lg:px-6 lg:pl-3 pb-3 lg:pb-6 gap-6">
    <div class="flex flex-col gap-10 max-w-full lg:max-w-[90ch]">
      {{#if (eq page.layout '404')}}
      {{> article-404}}
      {{else}}
      {{> article}}
      {{/if}}
      {{> footer}}
    </div>
    {{#unless (or (eq page.layout 'full') (eq page.layout 'landing'))}}
    {{> toc}}
    {{/unless}}
  </div>
</div>